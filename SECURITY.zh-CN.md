# 安全政策

## 支持范围

DearOreUI 目前处于早期开发阶段。安全问题主要覆盖最新发行版本和当前开发分支。运行时 Hook、资源处理、JavaScript 注入、Host API 和 UI 接入能力仍可能随着架构验证而变化。

## 报告漏洞

不要在公开 Issue 中披露疑似漏洞。请通过本仓库 GitHub 的私下 **Security** 报告渠道提交。

报告请包含：

- 受影响的 DearOreUI 版本或提交号。
- Minecraft、LeviLamina 和 Windows 版本。
- 受影响的页面、资源、API 或权限边界。
- 复现步骤或最小概念验证。
- 预期影响和已知缓解方式。
- 在可以提供时附上脱敏日志和诊断 ID。

除非确有必要，不要附加世界存档、游戏安装目录、私人服务器地址、访问令牌、玩家信息或未经脱敏的日志。必要时必须先移除敏感数据。

## 安全边界

DearOreUI 的设计要求包括：

- 资源按 Mod 命名空间隔离。
- 资源路径必须规范化，不能任意访问本地文件。
- Host API 必须显式注册并进行权限校验。
- 请求绑定页面上下文，页面销毁后立即失效。
- 高级变换必须校验版本和资源指纹。
- 目标不支持或校验失败时安全回退到原始页面。

绕过上述边界的问题具有较高价值。请在维护者完成调查前不要公开可用漏洞利用方式。
